const numeros = [1, 2, 3, 4, 5, 8];

// Desafio: remover o último elemento

numeros.pop();        // Remove o último
console.log(numeros);

numeros.splice(1, 2); // Remove por ÍNDICE
console.log(numeros);
